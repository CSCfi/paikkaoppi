export interface Task {
    id: number
    name: string
    title: string
    description: string
    info: string
    tags: string[]
    code: string
}

export interface TaskTemplate {
    id: number
    name: string
    title: string
    description: string
    info: string
    tags: string[]
}