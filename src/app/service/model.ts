export interface Task {
    name: string
    title: string
    description: string
    info: string
    tags: string[]
    code: string
}

export interface TaskTemplate {
    name: string
    title: string
    description: string
    info: string
    tags: string[]
}